"""Views & lists API — team/domain pages, task & artifact lists/details.

Wave-0 stub: exposes an empty `router` so app.py can pre-wire the include.
Endpoints are implemented by Wave-1 Agent 1B. Do NOT edit app.py to add routes.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api", tags=["views"])
