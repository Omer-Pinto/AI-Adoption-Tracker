import { PageStub } from '@/components/PageStub';

// Route: "/manage" — teams / champions / domains lists with Add/Edit. Wave-2 agent 2A.
export default function ManagePage() {
  return <PageStub title="Manage" subtitle="Teams, champions, domains" />;
}
