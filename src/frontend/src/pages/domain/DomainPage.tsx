import { PageStub } from '@/components/PageStub';

// Route: "/domains/:domainId" — single domain: current tasks/artifacts + story. Wave-2 agent 2B.
export default function DomainPage() {
  return <PageStub title="Domain" subtitle="Current state + week-by-week story" />;
}
