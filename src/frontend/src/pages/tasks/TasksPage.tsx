import { PageStub } from '@/components/PageStub';

// Route: "/tasks" — all tasks (search bar; rows expand to journey). Wave-2 agent 2D.
export default function TasksPage() {
  return <PageStub title="Tasks" subtitle="All tasks — week-by-week journey" />;
}
