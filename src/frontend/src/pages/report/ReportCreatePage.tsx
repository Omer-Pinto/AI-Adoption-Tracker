import { PageStub } from '@/components/PageStub';

// Route: "/reports/new" — paste raw notes → model drafts report. Wave-2 agent 2C.
export default function ReportCreatePage() {
  return <PageStub title="Create report" subtitle="Paste meeting notes → draft with the model" />;
}
