import { PageStub } from '@/components/PageStub';

// Route: "/reports/:reportId/edit" — edit a saved report's structured fields. Wave-2 agent 2C.
export default function ReportEditPage() {
  return <PageStub title="Edit report" subtitle="Edit structured fields (not the original notes)" />;
}
