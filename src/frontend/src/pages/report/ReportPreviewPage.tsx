import { PageStub } from '@/components/PageStub';

// Route: "/reports/:reportId/preview" — preview drafted report (not saved). Wave-2 agent 2C.
export default function ReportPreviewPage() {
  return <PageStub title="Preview report" subtitle="Not saved yet — review, fix, confirm" />;
}
