import { PageStub } from '@/components/PageStub';

// Route: "/artifacts" — artifacts registry (search bar + detail modal). Wave-2 agent 2D.
export default function ArtifactsPage() {
  return <PageStub title="Artifacts" subtitle="All artifacts across all domains" />;
}
