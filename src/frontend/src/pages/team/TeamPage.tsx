import { PageStub } from '@/components/PageStub';

// Route: "/teams/:championId" — one champion's portfolio, labeled by team. Wave-2 agent 2B.
export default function TeamPage() {
  return <PageStub title="Team" subtitle="Champion portfolio — domains, story, reports" />;
}
