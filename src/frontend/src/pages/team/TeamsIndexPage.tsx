import { PageStub } from '@/components/PageStub';

// Route: "/" — Teams index (list of champion portfolios). Wave-2 agent 2B.
export default function TeamsIndexPage() {
  return <PageStub title="Teams" subtitle="All team champion portfolios" />;
}
