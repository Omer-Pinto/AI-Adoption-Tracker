# Data Platform — Sven — Report 2 (2026-05-15)

## NOTES TO PASTE
```
Meeting with Sven, May 15th.
Multi-week tasks:
The CDC migration is stuck - the Debezium connector has a compatibility problem.
The Great Expectations checks are in progress now, due June 10th. Still Lior's.
Plan the data team's Q3 hiring. Sven owns it, not started.
Claude Code Artifacts:
Updated the dbt reviewer agent to also check test coverage.
New hook: a dbt-compile pre-push hook.
Action items / notes:
Documenting the dbt reviewer agent is underway.
Comments:
We're evaluating dbt Cloud vs self-hosted.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-15`  ·  **champion:** `Sven`  ·  **participants:** `[Sven]`

### Tasks
- **Migrate Kafka Connect to CDC for the orders table** — MATCHED to week-1 task, status → `blocked`, domain **Ingestion**. (`@`-mention "the CDC migration".)
- **Add Great Expectations checks to the revenue pipeline** — MATCHED to week-1 task, status → `in-progress`, owner stays `Lior`, **due_date `2026-06-10`** (PAST → overdue), domain **Data Quality**.
- **Plan the data team's Q3 hiring** — NEW, status `planned`, owner `Sven`, domain → **null → General** (non-technical, no domain fits). Intended **General** test.

### Artifacts
- **dbt model reviewer** — MATCHED to week-1 agent (id set, type copied = `agent`), change_kind `updated`, `note` = test-coverage check, domain **Transformation**. (`#`-mention.)
- **dbt-compile pre-push hook** — NEW, type **`hook`**, domain **Transformation**, change_kind `added`

### Action items
- "Document the dbt reviewer agent" — owner `Sven`, status `in-progress` (fresh row; week-1 `planned` unchanged)
  - ⚠️ **A1+A2:** champion (`Sven`) follow-up → now a **task**, not an action item. As a task it is **id-matched**, so this week-3 mention **UPDATES** the week-1 "Document the dbt reviewer agent" task to `in-progress` (NOT a fresh row — the opposite of the old action-item behavior).

### Discussion
- Evaluating dbt Cloud vs self-hosted.

### Issues
- (none new)

### Tricks to verify
- **`hook` type** covered; matched **agent** update.
- **Overdue task**: GE checks due 2026-06-10 + in-progress → overdue.
- **General**: the hiring task lands in General (watch the model does not invent a "Hiring" domain or force it into a tech domain).
