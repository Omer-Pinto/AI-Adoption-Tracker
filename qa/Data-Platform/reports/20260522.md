# Data Platform — Sven — Report 3 (2026-05-22)

## NOTES TO PASTE
```
Meeting with Sven, May 22nd.
Multi-week tasks:
Found a workaround - the CDC migration is finished and working.
The Great Expectations checks are done but a few expectations are flaky - done with issues. Lior's.
domain - Ingestion: We considered rebuilding ingestion in Flink but rejected it - won't do.
Claude Code Artifacts:
Built a new agent for dbt test generation (separate from the reviewer agent).
Action items / notes:
AI enablement lead to roll out the dbt reviewer agent to the BI team.
Comments:
Claude adoption survey results came back positive.
Our data-lineage tooling has a real gap.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-22`  ·  **champion:** `Sven`  ·  **participants:** `[Sven]`

### Tasks
- **Migrate Kafka Connect to CDC for the orders table** — MATCHED to existing task, status → `finished_successfully`, domain **Ingestion**. (`@`-mention.)
- **Add Great Expectations checks to the revenue pipeline** — MATCHED to existing task, status → `finished_with_issues`, owner stays `Lior`, domain **Data Quality**.
- **Rebuild ingestion in Flink** — NEW, status `wont_fix`, owner `Sven`, domain **Ingestion**.

### Artifacts
- **dbt test generator** (or similar) — NEW, type `agent`, domain **Transformation**, change_kind `added`. **"new …" convention**: despite the existing "dbt model reviewer" agent, the explicit "new agent … (separate from the reviewer agent)" MUST create a fresh artifact, NOT match the reviewer.

### Action items
- "Roll out the dbt reviewer agent to the BI team" — owner **`AI Lead`**, status `planned`, due null

### Discussion
- Claude adoption survey results came back positive.

### Issues
- Data-lineage tooling has a real gap.

### Tricks to verify
- **"new … despite a similar existing name"**: dbt test generator must NOT match/overwrite the dbt model reviewer agent — two distinct agents.
- **Task `wont_fix`** (Flink) and matched **finished_successfully** / **finished_with_issues** close out the Data Platform status coverage.
