# Data Platform — setup

**Team display name:** `Data Platform` (folder = team name)
**Champion(s):** `Sven` (single champion)

## What Omer creates MANUALLY (Manage page)
- The **team** `Data Platform`.
- The **champion** `Sven`.

## What is created via the LLM "Smart domain extract" flow
Paste under champion **Sven**, then accept:
```
1. Ingestion - airflow, kafka connect, python, cdc, debezium
2. Transformation - dbt, spark, sql, snowflake, modeling
3. Data Quality - great expectations, monitoring, lineage, freshness
Priority Order: 1 -> 2 -> 3
```
Expected (arrow = list order here): Ingestion = 1, Transformation = 2, Data Quality = 3.

## Auto-created domains (do NOT add)
**General** (fallback) and **Context creation** (real target) — minted automatically for Sven.

## Feed order
`20260508` → `20260515` → `20260522`. Save each before drafting the next.
