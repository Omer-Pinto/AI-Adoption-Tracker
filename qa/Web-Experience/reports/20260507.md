# Web Experience — Noa — Report 2 (2026-05-07)

## NOTES TO PASTE
```
Meeting with Noa, Web Experience champion (mobile side), May 7th.
Multi-week tasks:
domain - Mobile: Adopt Expo Router in the app. Not started.
domain - API Gateway: Add GraphQL persisted queries. In progress.
Claude Code Artifacts:
Context: I added a mobile-architecture context file - the navigation map and a native-module index.
Action items / notes:
Noa to spike Expo Router by June 25th.
Comments:
We're considering EAS for builds.
iOS build times are painfully slow.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-07`  ·  **champion:** `Noa`  ·  **participants:** `[Noa]`

### Tasks (NEW)
- **Adopt Expo Router in the app** — status `planned`, owner `Noa`, domain **Mobile**, due null
- **Add GraphQL persisted queries** — status `in-progress`, owner `Noa`, domain **API Gateway**, due null

### Artifacts (NEW)
- **Mobile architecture context** — type `context`, domain **Context creation**, change_kind `added`. GROUPING: navigation map + native-module index = ONE context artifact.

### Action items
- "Spike Expo Router" — owner `Noa`, status `planned`, **due_date `2026-06-25`** (PAST vs today → overdue)
  - ⚠️ **A1+A2:** champion (`Noa`) follow-up → now expected as a **task** (Mobile or General), NOT an action item (no owner on action items).

### Discussion
- Considering EAS for builds.

### Issues
- iOS build times are painfully slow.

### Tricks to verify
- **All four domains under Noa**: here Noa places into Mobile / API Gateway / her Context creation; in Report 1 she placed into Web Frontend / Design System — all the same champion's domains in one team.
- These mobile tasks are NEW; nothing here should match the checkout/palette tasks from Report 1 (different tasks, same champion).
- Noa's "Context creation" / "General" are the same single rows used in Report 1 (one champion, one of each).
