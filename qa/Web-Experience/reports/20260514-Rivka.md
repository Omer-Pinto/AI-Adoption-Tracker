# Web Experience — Rivka — Report 2 (2026-05-14)

## NOTES TO PASTE
```
Meeting with Rivka, May 14th.
Multi-week tasks:
We abandoned the Expo Router adoption - staying on React Navigation.
Persisted queries are finished and working well.
New task - evaluate Expo EAS Build for the mobile pipeline. Not started.
Claude Code Artifacts:
I wired the a11y lint skill into the mobile lint config.
Action items / notes:
AI enablement lead to compare EAS vs self-hosted CI for mobile.
Comments:
Team retro feedback on Claude was positive.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-14`  ·  **champion:** `Rivka`  ·  **participants:** `[Rivka]`

### Tasks
- **Adopt Expo Router in the app** — MATCHED to Rivka's week-1 task, status → `abandoned`, domain **Mobile**. (`@`-mention.)
- **Add GraphQL persisted queries** — MATCHED to Rivka's week-1 task, status → `finished_successfully`, domain **API Gateway**.
- **Evaluate Expo EAS Build** — NEW (explicit "new task"), status `planned`, owner `Rivka`, domain **Mobile**, due null.

### Artifacts
- **a11y lint** — **CROSS-CHAMPION MATCH**: this skill was created by DANIEL (team-wide). Because artifacts are team-scoped, it IS in Rivka's draft context, so the model must MATCH it by id (type copied = `skill`), change_kind `updated`, `note` = mobile lint wiring, **stays team-wide (domain null)** — NOT a new duplicate, NOT moved into Mobile. (`#`-mention.)

### Action items
- "Compare EAS vs self-hosted CI for mobile" — owner **`AI Lead`**, status `planned`, due null

### Discussion
- Team retro feedback on Claude was positive.

### Issues
- (none)

### Tricks to verify (the headline trick of this dataset)
- **Cross-champion artifact continuity**: the a11y lint skill Rivka references is the SAME row Daniel created. Verify ONE artifact with TWO history entries (Daniel CI integration + Rivka mobile wiring), still team-wide — no duplicate, no domain.
- **"new …"** on the EAS task forces a new entity (here there's no similar existing task, so it's simply NEW).
- Rivka's matched tasks (Expo Router, persisted queries) are HER own week-1 tasks, not Daniel's.
