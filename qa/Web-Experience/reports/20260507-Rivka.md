# Web Experience — Rivka — Report 1 (2026-05-07)

## NOTES TO PASTE
```
Meeting with Rivka, Web Experience champion (mobile side), May 7th.
Multi-week tasks:
domain - Mobile: Adopt Expo Router in the app. Not started.
domain - API Gateway: Add GraphQL persisted queries. In progress.
Claude Code Artifacts:
Context: I added a mobile-architecture context file - the navigation map and a native-module index.
Action items / notes:
Rivka to spike Expo Router by June 25th.
Comments:
We're considering EAS for builds.
iOS build times are painfully slow.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-07`  ·  **champion:** `Rivka`  ·  **participants:** `[Rivka]`

### Tasks (NEW)
- **Adopt Expo Router in the app** — status `planned`, owner `Rivka`, domain **Mobile**, due null
- **Add GraphQL persisted queries** — status `in-progress`, owner `Rivka`, domain **API Gateway**, due null

### Artifacts (NEW)
- **Mobile architecture context** — type `context`, domain **Context creation** (Rivka's OWN, separate from Daniel's), change_kind `added`. GROUPING: navigation map + native-module index = ONE context artifact.

### Action items
- "Spike Expo Router" — owner `Rivka`, status `planned`, **due_date `2026-06-25`** (PAST vs today → overdue)

### Discussion
- Considering EAS for builds.

### Issues
- iOS build times are painfully slow.

### Tricks to verify
- **Per-champion domains**: Rivka places into Mobile / API Gateway / her own Context creation — these are DIFFERENT domain rows from Daniel's, even though it's one team.
- Rivka does NOT see Daniel's tasks (task scope is per-champion), so nothing here should match Daniel's checkout/palette tasks.
- Rivka's "Context creation" / "General" are separate rows from Daniel's (per-champion constants).
