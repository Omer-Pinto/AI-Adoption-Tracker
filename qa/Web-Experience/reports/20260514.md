# Web Experience — Noa — Report 4 (2026-05-14)

## NOTES TO PASTE
```
Meeting with Noa, May 14th.
Multi-week tasks:
We abandoned the Expo Router adoption - staying on React Navigation.
Persisted queries are finished and working well.
New task - evaluate Expo EAS Build for the mobile pipeline. Not started.
Claude Code Artifacts:
I wired the a11y lint skill into the mobile lint config.
Action items / notes:
AI enablement lead to compare EAS vs self-hosted CI for mobile.
Comments:
Team retro feedback on Claude was positive.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-14`  ·  **champion:** `Noa`  ·  **participants:** `[Noa]`

### Tasks
- **Adopt Expo Router in the app** — MATCHED to Noa's Report 2 task, status → `abandoned`, domain **Mobile**. (`@`-mention.)
- **Add GraphQL persisted queries** — MATCHED to Noa's Report 2 task, status → `finished_successfully`, domain **API Gateway**.
- **Evaluate Expo EAS Build** — NEW (explicit "new task"), status `planned`, owner `Noa`, domain **Mobile**, due null.

### Artifacts
- **a11y lint** — **SAME-CHAMPION CROSS-WEEK MATCH**: this team-wide skill was created by Noa in Report 1. Because artifacts are team-wide it is in Noa's draft context, so the model must MATCH it by id (type copied = `skill`), change_kind `updated`, `note` = mobile lint wiring, **stays team-wide (domain null)** — NOT a new duplicate, NOT moved into Mobile. (`#`-mention.)

### Action items
- "Compare EAS vs self-hosted CI for mobile" — owner **`AI Lead`**, status `planned`, due null

### Discussion
- Team retro feedback on Claude was positive.

### Issues
- (none)

### Tricks to verify (the headline trick of this dataset)
- **Cross-week team-wide artifact continuity**: the a11y lint skill referenced here is the SAME row Noa created in Report 1. Verify ONE artifact with TWO history entries (Report 3 CI integration + Report 4 mobile wiring), still team-wide — no duplicate, no domain.
- **"new …"** on the EAS task forces a new entity (here there's no similar existing task, so it's simply NEW).
- Noa's matched tasks (Expo Router, persisted queries) are her own Report 2 tasks.
