# Payments Platform — Maya — Report 2 (2026-05-11)

## NOTES TO PASTE
```
Meeting with Maya, May 11th.
Multi-week tasks:
The idempotency work on refunds is done - shipped clean.
Fraud feature-store migration is now in progress, target date June 20th.
domain - Infrastructure: Pin Terraform provider versions. Tomer is taking this. Not started.
Claude Code Artifacts:
Extended the gRPC scaffold skill to also support streaming RPCs.
New hook: a pre-commit hook that blocks secrets sneaking into config files.
Action items / notes:
The gRPC scaffold how-to doc is done.
We decided NOT to push the gRPC scaffold skill org-wide - keeping it team-local.
Comments:
Team wants a shared prompt library.
```

## EXPECTED STRUCTURED OUTPUT
- **meeting_date:** `2026-05-11`  ·  **champion:** `Maya`  ·  **participants:** `[Maya]`

### Tasks
- **Add idempotency keys to the refund endpoint** — MATCHED to week-1 task (id set, exact name), status → `finished_successfully`, domain **Payments Core**. (`@`-mention: "the idempotency work" must resolve to the existing task, NOT a new one.)
- **Migrate the fraud rules engine onto the feature store** — MATCHED to week-1 task, status → `in-progress`, **due_date `2026-06-20`** (now set), domain **Risk & Fraud**. By today (2026-06-28) this date is in the PAST and the task is non-terminal → should render **overdue**.
- **Pin Terraform provider versions** — NEW (id null), status `planned`, owner **`Tomer`** (named DIFFERENT person → NOT defaulted to Maya), domain **Infrastructure**, due null

### Artifacts
- **gRPC scaffold generator** — MATCHED to week-1 artifact (id set, type copied = `skill`), change_kind `updated`, `note` = streaming-RPC support, domain **Payments Core**. (`#`-mention of an existing artifact → update, NOT a duplicate.)
- **Secrets pre-commit hook** (or similar) — NEW, type `hook`, domain **Infrastructure**, change_kind `added`

### Action items (NOTE: action items are per-report rows, NOT id-matched across weeks)
- "gRPC scaffold how-to doc" — owner `Maya`, status `finished_successfully`. This is a FRESH row; last week's `planned` row is unchanged. Verify it shows as finished.
- "Push gRPC scaffold skill org-wide" — owner **`AI Lead`**, status `wont_fix` (decided against). Fresh row.

### Discussion
- Team wants a shared prompt library.

### Issues
- (none this week)

### Tricks to verify
- **Matched-task continuity**: idempotency + fraud tasks must UPDATE in place (no duplicate rows).
- **Sticky due_date set later**: fraud task gets its first due_date in week 2; verify it sticks (a later report that omits it must not wipe it).
- **Overdue**: fraud task due 2026-06-20 + in-progress → overdue badge.
- **Different-person owner**: Terraform task owner = `Tomer`, not Maya.
- **Action-item status "change"** is really a NEW row with the new status — confirm the model does NOT try to merge with week-1, and watch for whether the AI-Lead view shows two rows (possible duplicate concern — flag if it bothers you).
