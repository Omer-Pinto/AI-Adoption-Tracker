# QA run — Web Experience — Report 2 (2026-05-07)

## ⚠️ Findings
1. **[LLM — real, SERIOUS over-inclusion / NEW bug not in round-1]** The draft re-emitted **prior-week entities that are NOT in these notes**: tasks "Tokenize the color palette" (id 9), "Migrate the checkout flow" (id 8), "Schedule an accessibility training session" (id 10), and artifacts "A11y lint skill" (id 6, change=updated), "Storybook conventions doc" (id 5, change=updated). The 05-07 notes only mention the mobile/API work. qa README trick explicitly says "nothing here should match the checkout/palette tasks from Report 1." The model pulled in the whole prior entity list and would have written spurious `task_history`/`artifact_history` rows dated 05-07 for undiscussed items. **Hypothesis:** the "Multi-week tasks:" header may prompt the model to re-list ongoing tasks. **Corrected before save** (dropped all id-matched entities; kept only the genuinely-new ones).
2. **[LLM — real] context file misplaced.** "mobile-architecture context file" → domain **Mobile**; expected **Context Creation** (context files are Claude tooling; Payments R1 & Web R1 context files both went to Context Creation — inconsistent here). **Corrected** → Context Creation.
3. **[LLM — real] dropped due_date.** "Noa to spike Expo Router **by June 25th**" → task created but `due_date` null. **Corrected** → 2026-06-25.

## ✅ Works
- New tasks correct: Adopt Expo Router→Mobile (planned), Add GraphQL persisted queries→API Gateway (in-progress), both owner Noa.
- **A1+A2:** "Noa to spike Expo Router" → **task** (Noa), NOT an action item; action_items empty (correct).
- Mobile context = ONE artifact (grouping of nav-map + native-module index).
- discussion (EAS) / issues (iOS build times) correct.

## Saved (after correction)
report id 5; kept 3 new tasks + 1 mobile-context artifact (Context Creation).
