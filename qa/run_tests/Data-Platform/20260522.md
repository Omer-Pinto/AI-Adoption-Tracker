# QA run — Data Platform — Report 3 (2026-05-22)

## ✅ Works
- Matched: CDC migration (id 15) → `finished_successfully`; GE checks (id 16) → `finished_with_issues` (owner stays Lior).
- **D1 no-drop:** "considered rebuilding ingestion in Flink but rejected it — won't do" → NEW task "Rebuild ingestion in Flink", `wont_fix`, Sven, Ingestion. ✅
- **"new … despite similar existing name":** "new agent for dbt test generation (separate from the reviewer agent)" → NEW `agent`, did NOT match/overwrite the existing dbt reviewer. ✅
- **AI-Lead action item:** "Roll out the dbt reviewer agent to the BI team" → action_items (no owner). ✅
- discussion (survey positive) / issues (data-lineage gap) correct. ✅

## ⚠️ Findings
1. **[LLM — real, over-inclusion — 2nd occurrence]** the dbt model reviewer (id 10) was re-emitted with change_kind **`updated`** even though it is only *referenced* ("separate from the reviewer agent"), not changed → would write a spurious `artifact_history` "updated" row on 05-22. **Corrected before save** (dropped; kept only the new test-generation agent). Same over-inclusion pattern as Web R2 — the model re-emits a prior entity whenever its name is mentioned.
2. **[expected STALE per A4]** dbt test generator (agent) → Context Creation; md expected Transformation. Design-correct.

Data Platform totals: 3 reports · 5 tasks (2 open / 3 closed) · 4 artifacts.
