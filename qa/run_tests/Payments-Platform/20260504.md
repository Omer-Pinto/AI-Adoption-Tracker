# QA run — Payments Platform — Report 1 (2026-05-04)

Live draft via `POST /api/reports/draft` (real OpenAI, 1-1 with UI). Compared to the report md's expected output.

## ✅ A1+A2 / D behaviors that WORK
- **A1+A2 / D2 split correct:** "Maya to write a how-to doc" → extracted as a **task** (owner `Maya`), NOT an action item. "AI enablement lead to evaluate org-wide" → **AI-Lead action item** with **no owner** and the clean contract `{text, note, status, due_date}` (no owner/domain/team keys). Both routed correctly.
- **D5/D8 owners:** all 4 tasks carry owner `Maya` in the DRAFT (no blank "— owner —").
- **D3 change_kind:** both artifacts `added`.
- **D6 summaries:** both artifacts have descriptive summaries; context artifact GROUPED the 3 md files into ONE (`context`) — grouping works.
- **Domains:** idempotency→Payments Core, fraud→Risk & Fraud, PagerDuty→Observability — all correct.

## ⚠️ Findings
1. **[LLM — real] Duplicate/over-capture into `discussion`.** The draft added two spurious discussion entries: (a) "Claude Code Artifacts were discussed including … context files and a gRPC scaffold generator skill" — a **restatement of the two artifacts** already captured; (b) "Maya is to write a short how-to doc…" — a **duplicate of the how-to-doc task**. Expected discussion is ONLY "Company marketplace adoption is still low." → over-capture / no-overlap violation. **Corrected before save** (removed both).
2. **[LLM — borderline D7] issues vs discussion.** "Company marketplace adoption is still low" landed in **issues**; expected **discussion** (a neutral status remark, not a risk). "On-call fatigue creeping up" → issues ✓. qa README uncertainty #7 flags this as a judgment call. **Corrected before save** (moved marketplace→discussion).
3. **[expected STALE per A4 — draft is CORRECT, not a bug] skill/doc → Context Creation.** The gRPC scaffold **skill** and the "write a how-to doc" **task** both landed in **Context Creation**. The report md expected Payments Core / General, but that predates A4 (Context Creation = ALL Claude tooling + docs about building it). Draft matches current design → **kept as Context Creation**; the qa md expectation is stale (already flagged in bug-list A4).

## Saved state (after correction)
4 tasks (owner Maya), 2 artifacts (both Context Creation, `added`), 1 AI-Lead action item (no owner, team=Payments Platform stamped at fan-out), discussion=[marketplace], issues=[on-call fatigue].
