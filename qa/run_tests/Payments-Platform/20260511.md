# QA run — Payments Platform — Report 2 (2026-05-11)

## ✅ Works
- **Matched-task continuity:** idempotency → `finished_successfully` (id 1), fraud → `in-progress` (id 2) — updated in place, no duplicates.
- **Sticky due_date:** fraud task got due `2026-06-20` (first set here).
- **Different-person owner (D5):** Terraform task owner = `Tomer`, NOT defaulted to Maya.
- **Artifact match + type-copy:** gRPC skill matched (id 2), `change_kind=updated`, note = streaming-RPC support, type `skill` copied — not duplicated.
- **D6 name/summary:** hook named **"Secrets pre-commit hook"** (not generic "pre-commit hook") + summary "Blocks secrets from being committed…".
- **AI-Lead action items not deduped:** after correction the board shows BOTH "Evaluate…org-wide" (planned, wk1) and "Push…org-wide" (wont_fix, wk2) — expected per qa README.

## ⚠️ Findings
1. **[LLM — real, matching miss]** "The gRPC scaffold how-to doc is done" was routed to **discussion** instead of MATCHING the week-1 how-to-doc **task** and setting it `finished_successfully`. The A1+A2 rule (champion follow-up = task, id-matched across weeks) requires the model to recognize a task-completion line; it treated it as a discussion remark. **Corrected before save** (matched task 4 → finished).
2. **[LLM — real, miss]** "We decided NOT to push the gRPC scaffold skill org-wide" was routed to **discussion** instead of a fresh **AI-Lead action item** with status `wont_fix`. **Corrected before save** (added the wont_fix AI-Lead item).
   - PATTERN: both "Action items / notes:" lines were dumped into discussion — the model under-recognizes task-completions and AI-Lead decisions when phrased as statements.
3. **[expected STALE per A4 — draft correct]** gRPC skill (updated) and Secrets hook (new) both → **Context Creation**; md expected Payments Core / Infrastructure (pre-A4). Kept design-correct.
   - **PRODUCT OBSERVATION for Omer:** A4 sends ALL Claude tooling (skill/agent/hook/context) to Context Creation — and every artifact is one of those types — so under A4 **artifacts essentially never land in a tech domain**. If that's not intended, A4's scope for artifacts (vs tasks/action-items about building tooling) may need revisiting.

## Self-inflicted note (not an app bug)
When correcting finding #1 I passed a null domain on the matched task → the engine's task-fallback re-placed it to General; I re-placed it to Context Creation via `PATCH /tasks`. (Confirms matched-task placement is report-authoritative — expected engine behavior.)
